# 搭建使用说明

本文说明如何从 GitHub 下载并运行本微信小程序项目。

## 1. 准备工具

需要先安装：

- 微信开发者工具
- Git
- 一个微信小程序 AppID，或使用微信开发者工具的测试号

项目是纯本地版，不需要安装 Node.js，不需要数据库，也不需要微信云开发。

## 2. 下载项目

打开命令行，执行：

```bash
git clone https://github.com/Twb1994/worktime.git
```

进入项目目录：

```bash
cd worktime
```

如果不想使用 Git，也可以在 GitHub 页面点击 `Code`，选择 `Download ZIP`，下载后解压。

## 3. 导入微信开发者工具

1. 打开微信开发者工具。
2. 点击“导入项目”。
3. 项目目录选择下载后的 `worktime` 文件夹。
4. AppID 可以填写自己的小程序 AppID，也可以选择测试号。
5. 后端服务选择“不使用云服务”。
6. 点击“导入”。

## 4. 编译运行

导入后点击“编译”。

正常情况下可以看到底部 4 个页面：

- 记工
- 记录
- 统计
- 工人

首次打开会自动创建一个“默认工人”，可以在“工人”页面修改或删除。

## 5. 基本使用流程

1. 进入“工人”页面，添加工人并设置工资。
2. 进入“记工”页面，选择日期、工人、正常工时，按需填写加班和备注。
3. 保存后进入“记录”页面查看历史记录。
4. 进入“统计”页面选择日期范围，或按月快速筛选。
5. 点击“导出 CSV”导出当前统计范围内的数据。

## 6. 数据保存说明

本项目使用微信小程序本地缓存保存数据。

注意：

- 不会上传到服务器。
- 不会自动同步到其他手机。
- 清理小程序缓存后，本地数据可能丢失。

## 7. 常见问题

### 导入时提示 AppID

可以使用自己的小程序 AppID，也可以选择测试号。测试号适合本地调试。

### 是否需要开通云开发

不需要。导入项目时选择“不使用云服务”。

### CSV 文件在哪里

导出成功后，小程序会把 CSV 文件路径复制到剪贴板。可以在开发者工具或手机文件环境中按路径查找。

### 中文乱码怎么办

CSV 已带 UTF-8 BOM，正常情况下 Excel 和 WPS 可以直接识别中文。如果仍乱码，可以用 WPS 或选择 UTF-8 编码打开。

## 8. 发布前检查

发布或提交代码前建议检查：

- 微信开发者工具可以正常编译。
- “工人”页面能添加工人。
- “记工”页面能保存记录。
- “统计”页面能按日期和月份统计。
- CSV 导出能生成文件。
- `project.private.config.json` 不要提交到 GitHub。
